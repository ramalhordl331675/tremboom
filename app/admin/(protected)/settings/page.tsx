import { SectionPlaceholder } from "@/components/admin/SectionPlaceholder";

export default function AdminSettingsPage() {
  return (
    <SectionPlaceholder
      title="Configurações"
      description="Ajuste as preferências e o comportamento do painel administrativo."
    />
  );
}
